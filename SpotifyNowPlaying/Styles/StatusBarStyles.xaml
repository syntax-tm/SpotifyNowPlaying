<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
        <ResourceDictionary Source="TextStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="StatusBarBaseStyle" TargetType="{x:Type StatusBar}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXSm}" />
    </Style>

    <Style x:Key="StatusBarSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Foreground" Value="{StaticResource StatusBarForegroundBrush}" />
    </Style>

    <Style x:Key="StatusBarLabelStyle" TargetType="{x:Type Label}" BasedOn="{StaticResource LabelBaseStyle}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXSm}" />
        <Setter Property="Foreground" Value="{StaticResource StatusBarForegroundBrush}" />
    </Style>

</ResourceDictionary>